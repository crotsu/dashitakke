<p id="notice"><%= notice %></p>

<p>
  <strong>Index:</strong>
  <%= @paper.index %>
</p>

<p>
  <strong>Given date:</strong>
  <%= simple_date(@paper.given_date) %>
</p>

<p>
  <strong>Set:</strong>
  <%= @paper.set %>
</p>

<% if @paper.set %>出題済<% else %>未<% end %>
<br>
<% if @paper.set %>
  <%= link_to '取り消し', reset_paper_path(@paper.id), method: :put, class: "btn btn-danger" %>
<% else %>
  <%= link_to '出題', set_paper_path(@paper.id), method: :put, class: "btn btn-primary" %>
<% end %>

<hr>
<% if @paper.set %>

<%= render 'papers/students' %>

<% else %>
<hr>
<table class="table table-hover">
  <thead>
    <tr>
    	<th>問題番号</th>
    	<th>要チェック</th>
    	<th>ポイント</th>
    	<th>質問内容</th>
    	<th colspan="2"></th>
    </tr>
</thead>
<tbody>
	<% @paper.questions.each do |question| %>
	<tr>
		<td><%= question.index %></td>
		<td><%= question.need_check %></td>
		<td><%= question.point %></td>
		<td><%= question.contents %></td>
		<td><%= link_to '編集',
                { controller: :questions,
                  action: :edit,
                  :id => question },
                  :class => "btn btn-sm btn-warning" %>
        <%= link_to '削除',
                { controller: :questions,
                  action: :destroy,
                  :id => question },
                  :data => { :confirm => 'Are you sure?' },
                  :class => "btn btn-sm btn-danger",
                  :method => 'delete' %></td>
	<% end %>
  </tr>
</tbody>
</table>
<hr>

<%= bootstrap_form_for @question, :url => {controller: :questions, action: :create, :paper_id => @paper} do |f| %> 

	<div class="field">
		<%= f.label :index %>
    <p><%= @question.index %></p>
		<%= f.hidden_field :index %>
	</div>

	<div class="field">
		<%= f.select :need_check, ["なし","教員", "学生"] %>
	</div>

	<div class="field">
		<%= f.number_field :point %>
	</div>

	<div class="field">
		<%= f.text_area :contents %>
	</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<hr>

<%= link_to 'Edit', edit_paper_path(@paper) %> |
<%= link_to 'Back', papers_path %>

<% end %>
